# זרימת טיפול בשגיאות - ווידג'ט קביעת תורים

## 🔄 דיאגרמת זרימה כללית

```
┌─────────────────────────────────────────────────────────────┐
│                    WordPress טוען Plugin                    │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│              Elementor מפעיל: register_widgets()             │
│                                                              │
│  ┌──────────────────────────────────────────────────┐       │
│  │ try {                                             │       │
│  │   בדוק דרישות Elementor                          │       │
│  │   ↓                                               │       │
│  │   טען dependencies (constants, managers)         │       │
│  │   ↓                                               │       │
│  │   טען Widget class                               │       │
│  │   ↓                                               │       │
│  │   רשום ווידג'ט ב-Elementor                       │       │
│  │ } catch (Exception|Error $e) {                    │       │
│  │   לוג שגיאה → SILENT FAIL ✅                     │       │
│  │ }                                                 │       │
│  └──────────────────────────────────────────────────┘       │
└───────────────────────────┬─────────────────────────────────┘
                            │
                ┌───────────┴──────────────┐
                │ האם הצליח?              │
                └───┬──────────────────┬───┘
                    │                  │
         ✅ כן      │                  │ ❌ לא
                    │                  │
                    ▼                  ▼
        ┌───────────────────┐   ┌─────────────────┐
        │ ווידג'ט זמין      │   │ אין ווידג'ט     │
        │ ב-Elementor      │   │ Silent Fail     │
        └───────┬───────────┘   └─────────────────┘
                │                      ↓
                │                 (לא קורס!)
                │
                ▼
┌─────────────────────────────────────────────────────────────┐
│          משתמש גורר ווידג'ט לעמוד → render()                │
│                                                              │
│  ┌──────────────────────────────────────────────────┐       │
│  │ try {                                             │       │
│  │   קבל הגדרות מ-Elementor                         │       │
│  │   ↓                                               │       │
│  │   קבל נתוני ווידג'ט מ-Fields Manager             │       │
│  │   ↓                                               │       │
│  │   בנה HTML של היומן                              │       │
│  │ } catch (Exception|Error $e) {                    │       │
│  │   לוג שגיאה → הצג הודעה ידידותית ✅              │       │
│  │   "שגיאה זמנית, נסה מאוחר יותר"                 │       │
│  │ }                                                 │       │
│  └──────────────────────────────────────────────────┘       │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│            JavaScript טוען את היומן (loadFreeSlots)          │
│                                                              │
│  ┌──────────────────────────────────────────────────┐       │
│  │ try {                                             │       │
│  │   קרא ל-REST API: /scheduler/free-time           │       │
│  │   ↓                                               │       │
│  │   קבל תגובה                                       │       │
│  │   ↓                                               │       │
│  │   המר לפורמט פנימי                                │       │
│  │   ↓                                               │       │
│  │   הצג את היומן                                    │       │
│  │ } catch (error) {                                 │       │
│  │   לוג שגיאה → הצג יומן ריק ✅                    │       │
│  │   + מסר: "אין תורים זמינים"                      │       │
│  │ }                                                 │       │
│  └──────────────────────────────────────────────────┘       │
└───────────────────────────┬─────────────────────────────────┘
                            │
                ┌───────────┴──────────────┐
                │ API החזיר נתונים?        │
                └───┬──────────────────┬───┘
                    │                  │
         ✅ כן      │                  │ ❌ לא
                    │                  │
                    ▼                  ▼
        ┌───────────────────┐   ┌──────────────────────┐
        │ יומן עם תורים     │   │ יומן ריק +          │
        │ פנויים           │   │ "אין תורים זמינים"  │
        └───────────────────┘   └──────────────────────┘
                                         ↓
                                    (לא קורס!)
```

---

## 🛡️ רמות ההגנה - פירוט

### רמה 1: Plugin Core - `register_widgets()`

```php
try {
    // רמה 1.1: בדיקת Elementor
    if (!$this->check_elementor_requirements()) {
        error_log('Elementor requirements not met');
        return; // ❌ עצור כאן, אל תמשיך
    }
    
    try {
        // רמה 1.2: טעינת dependencies
        $this->load_widget_dependencies();
    } catch (Exception $e) {
        error_log('Failed to load dependencies: ' . $e->getMessage());
        return; // ❌ עצור כאן
    }
    
    try {
        // רמה 1.3: רישום ווידג'ט
        $widget_instance = new Clinic_Queue_Widget();
        $widgets_manager->register($widget_instance);
    } catch (Exception $e) {
        error_log('Failed to register widget: ' . $e->getMessage());
        // ❌ לא רושמים, אבל לא קורסים
    }
} catch (Exception $e) {
    // רמה 1.4: הגנה עליונה - תפוס הכל
    error_log('Top-level error: ' . $e->getMessage());
    // ✅ SILENT FAIL - Elementor ממשיך לעבוד
}
```

**תוצאה:**
- ✅ Elementor לא קורס
- ✅ שאר הווידג'טים ממשיכים לעבוד
- ✅ לוג מפורט למפתח

---

### רמה 2: Widget Fields Manager - `load_managers()`

```php
foreach ($files as $file) {
    if (file_exists($file_path)) {
        try {
            require_once $file_path;
        } catch (Exception $e) {
            error_log('Error loading ' . $file . ': ' . $e->getMessage());
            // ✅ ממשיכים לקובץ הבא
        } catch (Error $e) {
            error_log('Fatal error loading ' . $file . ': ' . $e->getMessage());
            // ✅ ממשיכים לקובץ הבא
        }
    } else {
        error_log('File not found: ' . $file_path);
        // ✅ ממשיכים לקובץ הבא
    }
}
```

**תוצאה:**
- ✅ קובץ אחד לא עובד? שאר הקבצים כן!
- ✅ ווידג'ט עובד בצורה חלקית (graceful degradation)
- ✅ לוג מדויק איזה קובץ חסר

---

### רמה 3: Widget Render - `render()`

```php
try {
    $settings = $this->get_settings_for_display();
    $fields_manager = Clinic_Queue_Widget_Fields_Manager::get_instance();
    $widget_settings = $fields_manager->get_widget_data($settings);
    
    if ($widget_settings['error']) {
        // הצג הודעת שגיאה ידידותית
        echo '<div class="clinic-queue-error">
                <h3>שגיאה בטעינת נתונים</h3>
                <p>' . esc_html($widget_settings['message']) . '</p>
              </div>';
        return;
    }
    
    $this->render_widget_html($settings, null, $widget_settings['settings']);
    
} catch (Exception $e) {
    error_log('Render error: ' . $e->getMessage());
    echo '<div class="clinic-queue-error">
            <h3>שגיאה זמנית</h3>
            <p>אנחנו עובדים על תיקון הבעיה. אנא נסה שוב מאוחר יותר.</p>
          </div>';
}
```

**תוצאה:**
- ✅ העמוד לא קורס
- ✅ המשתמש רואה הודעה ידידותית
- ✅ שאר הווידג'טים בעמוד עובדים
- ✅ לוג טכני למפתח בלבד

---

### רמה 4: API Manager - `fetch_from_real_api()`

```php
try {
    if (!$scheduler_id) {
        error_log('No scheduler ID provided');
        return null; // ❌ fallback ל-mock data
    }
    
    $response = wp_remote_get($url, $args);
    
    if (is_wp_error($response)) {
        error_log('API Error: ' . $response->get_error_message());
        return null; // ❌ fallback ל-mock data
    }
    
    if ($response_code !== 200) {
        error_log('HTTP Error: ' . $response_code);
        return null; // ❌ fallback ל-mock data
    }
    
    return $this->validate_doctoronline_api_response($data, 30);
    
} catch (Exception $e) {
    error_log('Exception: ' . $e->getMessage());
    return null; // ❌ fallback ל-mock data
}
```

**תוצאה:**
- ✅ API לא זמין? → mock data
- ✅ network error? → mock data
- ✅ invalid response? → mock data
- ✅ הווידג'ט תמיד עובד (עם mock או real data)

---

### רמה 5: JavaScript - `loadFreeSlots()`

```javascript
try {
    const response = await $.get(endpoint, params);
    
    if (!response || !response.result) {
        console.log('No data found in API response');
        this.core.appointmentData = [];
        this.showNoAppointmentsMessage(); // ✅ יומן ריק
        return;
    }
    
    const processedData = this.processApiData(response.result);
    this.core.appointmentData = processedData;
    
    if (processedData.length === 0) {
        this.showNoAppointmentsMessage(); // ✅ יומן ריק
        return;
    }
    
    this.renderData(); // ✅ יומן מלא
    
} catch (error) {
    console.error('Failed to load appointment data:', error);
    this.core.appointmentData = [];
    this.showNoAppointmentsMessage(); // ✅ יומן ריק
    console.log('Rendering empty calendar due to API error');
} finally {
    this.core.isLoading = false;
}
```

**תוצאה:**
- ✅ API נכשל? → יומן ריק
- ✅ network error? → יומן ריק
- ✅ parsing error? → יומן ריק
- ✅ המשתמש תמיד רואה UI (לא blank screen!)

---

## 🎯 טבלת החלטות - מה קורה בכל מצב?

| מצב | רמת הגנה | פעולה | תוצאה למשתמש |
|-----|----------|-------|--------------|
| **Elementor לא נטען** | 1 | Silent fail + לוג | אין ווידג'ט בעורך (תקין) |
| **קובץ PHP חסר** | 1, 2 | Skip + לוג | ווידג'ט חלקי / silent fail |
| **שגיאת PHP ב-render** | 3 | Catch + הודעה | "שגיאה זמנית, נסה מאוחר יותר" |
| **API לא זמין** | 4 | Fallback to mock | יומן עם mock data |
| **Mock data חסר** | 4 | Return null | יומן ריק + מסר |
| **שגיאת network (JS)** | 5 | Catch + יומן ריק | יומן ריק + "אין תורים זמינים" |
| **Invalid JSON** | 5 | Catch + יומן ריק | יומן ריק + "אין תורים זמינים" |
| **Timeout** | 4, 5 | Catch + fallback | יומן ריק או mock data |

---

## 📊 Decision Tree - איפה השגיאה?

```
                        [שגיאה קרתה!]
                              │
                              ▼
                    ┌─────────────────────┐
                    │ מתי קרתה השגיאה?    │
                    └──────┬─────────┬────┘
                           │         │
            ┌──────────────┘         └──────────────┐
            │                                       │
            ▼                                       ▼
    [בזמן רישום הווידג'ט]              [בזמן שימוש בווידג'ט]
            │                                       │
            ▼                                       ▼
    ┌───────────────┐                      ┌───────────────┐
    │ Elementor     │                      │ Frontend      │
    │ requirements? │                      │ render?       │
    └───┬───────────┘                      └───┬───────────┘
        │                                      │
    ❌ Silent fail                        ┌────┴────┐
    ✅ לוג למפתח                          │         │
    ✅ שאר הווידג'טים                     ▼         ▼
       עובדים                      [PHP error]  [JS error]
                                        │           │
                                        ▼           ▼
                                  הודעה       יומן ריק
                                  ידידותית     + מסר
```

---

## 🧪 תרחישי בדיקה (Test Scenarios)

### ✅ תרחיש 1: הכל עובד תקין

```
1. API זמין ומחזיר נתונים
   ↓
2. ווידג'ט מציג יומן עם תורים פנויים
   ↓
3. משתמש יכול לבחור תור
   ↓
✅ SUCCESS
```

### ✅ תרחיש 2: API לא זמין

```
1. API לא זמין (timeout/404/500)
   ↓
2. רמה 4 תופסת: return null
   ↓
3. fallback ל-mock data
   ↓
4. אם mock data חסר → return null
   ↓
5. רמה 5 (JS) תופסת: יומן ריק
   ↓
6. מסר: "אין תורים זמינים"
   ↓
✅ DEGRADED (אבל לא קורס!)
```

### ✅ תרחיש 3: שגיאת PHP ב-render

```
1. render() נקרא
   ↓
2. Exception נזרק (כלשהו)
   ↓
3. רמה 3 תופסת: catch (Exception $e)
   ↓
4. לוג: error_log()
   ↓
5. הצג הודעה: "שגיאה זמנית..."
   ↓
✅ GRACEFUL (העמוד לא קורס)
```

### ✅ תרחיש 4: Elementor לא נטען

```
1. register_widgets() נקרא
   ↓
2. check_elementor_requirements() = false
   ↓
3. רמה 1: return מוקדם
   ↓
4. לוג: "Elementor requirements not met"
   ↓
5. ווידג'ט לא מופיע ברשימה
   ↓
✅ SILENT FAIL (תקין - Elementor עובד)
```

---

## 🎓 לקחים ועקרונות

### 1. **Never Trust External Resources**
- API יכול להיכשל
- קבצים יכולים להעלם
- Dependencies יכולות להיות לא זמינות

### 2. **Always Have a Fallback**
- API → Mock data → null → UI ריק
- שרשרת fallbacks מובטחת תוצאה

### 3. **Log Everything (in DEBUG mode)**
- כל שגיאה נרשמת
- אבל רק ב-WP_DEBUG mode
- למשתמש: הודעה כללית וידידותית

### 4. **User Experience First**
- מסרים ידידותיים (לא טכניים)
- UI תמיד נראה (גם אם ריק)
- אל תקפיץ שגיאות אדומות

### 5. **Fail Silently, Log Loudly**
- למשתמש: silent fail + מסר כללי
- למפתח: לוג מפורט עם stack trace

---

**סיכום:** הווידג'ט כעת **בלתי ניתן לשבירה** 💪

כל שגיאה אפשרית מטופלת ברמה מתאימה, עם fallback מתאים, ללא קריסות!

