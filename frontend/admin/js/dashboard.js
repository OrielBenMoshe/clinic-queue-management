/**
 * Dashboard admin page script (frontend/admin)
 * Add custom behavior for the clinic queue dashboard here.
 *
 * @package Clinic_Queue_Management
 */

(function() {
	'use strict';
	// Placeholder – extend as needed (e.g. widget preview, stats refresh)
})();
